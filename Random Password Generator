# import random

# password = ""

# for _ in range(3):
#     upper = chr(random.randint(65, 90))        # A-Z
#     lower = chr(random.randint(65, 90)).lower()  # a-z
#     digit = str(random.randint(1, 9))          # 1-9 (not 0)

#     password += upper + lower + digit

# print("Generated password:", password)
